# Adversarial Review

## Verdict

Needs revision

## Findings

| Severity | Area | Finding | Evidence | Recommendation |
| --- | --- | --- | --- | --- |
| Major | State invalidation | A target edit can occur while analysis is in progress. The plan clears settled derived state, but it does not cancel the active request or reject a late result. A response for the old target can therefore run `analysisLoaded` after `jobDescriptionUpdated` and set `analysisStale` to false. | Plan lines 137-141 and 246-251; `frontend/src/SessionContext.tsx` lines 96-119 and 168-180; `frontend/src/components/ReviewStep.tsx` lines 52-57 show that editor changes are not blocked by `busy`. | Make target updates cancel the active derived request, or add a request generation key that guards every completion. Add a delayed-analysis browser or context test: edit the target before the old response returns, then prove that the old result cannot advance the stage or become current. Apply the same guard to rewrite and DOCX completions if those requests can overlap future source changes. |
| Major | CI fixture | The planned committed synthetic PDF conflicts with the repository ignore rule. `*.pdf` is ignored, so `tests/fixtures/integration_resume.pdf` will not reach CI unless the implementation force-adds it. The plan does not include an exact ignore exception. | Plan lines 168 and 174-176; `.gitignore` contains `*.pdf`. | Prefer runtime generation with the existing `reportlab` development dependency. If a binary fixture is necessary, add one exact allow rule such as `!tests/fixtures/integration_resume.pdf` and test that `git check-ignore` does not exclude it. Do not use a broad PDF exception. |
| Major | Product documentation | The plan adds target editing but does not explicitly update the current-functionality contract for that behavior. `FUNCTIONAL_OVERVIEW.md` would still say that the target cannot be reviewed or edited after ingestion. Phase 5 only specifies project-bullet corrections. | `FUNCTIONAL_OVERVIEW.md` lines 117-119 and 261-277; plan lines 240-269 and 325-354. | Update the workflow, state, error-recovery, and current-limitations text for target editing and invalidation. Include these edits in Phase 3 or Phase 5 and in the acceptance matrix. Keep all new technical text in ADS-STE100 Simplified Technical English. |
| Major | Required sequencing | Ponytail Full is a required pre-edit gate, but the plan places it in Phase 6 after five implementation phases. An implementation worker that follows phase order cannot satisfy the requirement. | Work brief requirement: run Ponytail Full before edits; plan lines 160-356 define implementation phases, while line 362 schedules the pre-edit run. | Move Ponytail Full to a Phase 0 preflight before dependency, fixture, code, or document edits. In Phase 6, verify the recorded result and run only the complete-diff Ponytail Review. Treat the two Impeccable references as one detector run after UI completion. |
| Minor | Real-stack test and DOCX parity | The integration scenario permits rewrite application to be skipped. It can pass without proving that accepted wording crosses the React model, request body, generator, and downloaded DOCX. The byte comparison also proves only that the same retained Blob downloads twice, which is valid for “Download again” but not a second generation. | Plan lines 189-197 and 384-385; current `frontend/src/SessionContext.tsx` lines 264-285 reuse one Blob for the second download. | Require the scenario to request both a work and project rewrite, apply at least one alternative, and assert that the DOCX contains the applied text and excludes the replaced text. State clearly that equal download bytes test retained-Blob repeat download, not independent generator determinism. Keep generator determinism in focused Python tests. |
| Minor | Integration harness realism | The plan does not define the working directory or static asset selection for the test server. `analyzer.py` prefers `public/` over `frontend/dist`, and `public/` is ignored. A stale local `public/` directory can make the test serve an old SPA even after a new frontend build. Starting Uvicorn from `frontend/` can also make both the Python module path and relative static paths wrong. | Plan lines 193-198; `analyzer.py` lines 737-744; `.gitignore` ignores `/public/`; the Playwright configuration runs from `frontend/`. | Define one cross-platform launch command with the repository root as the Uvicorn working directory. Before startup, make the served asset directory deterministic: copy the just-built output to a clean test target, or make the test app serve the exact new `frontend/dist`. Add an assertion that the server uses the new build and never reuses an existing process. |
| Minor | GSAP and reduced motion | The motion criteria are good, but the test cases are not precise enough to prove GSAP cleanup and reduced-motion behavior. The current accessibility test checks only CSS `animationDuration`; that check will not detect an inline transform or opacity left by GSAP. Analysis arrival can also overlap the stage-entry timeline unless the targets are separated. | Plan lines 281-287 and 311-323; `frontend/e2e/accessibility.spec.ts` lines 7-30; `frontend/src/styles.css` lines 343-355 and 2262-2270. | Add named tests for normal motion, reduced motion, forward/back stage changes, React Strict Mode replay, and interrupted transitions. Under reduced motion, assert final computed opacity and no transform or position change. Define the analysis acknowledgement target so that it does not replay the whole stage transition. Trigger export completion motion from the completed DOCX state, not only from truth-check readiness. |
| Minor | Deployment evidence | The private deployment rule is safe, but the plan lists anonymous browser verification without putting it in the release evidence or assigning it as an external gate. A local test suite cannot prove a provider dashboard setting. Checking only `/` and the non-sensitive config route also leaves the intended API denial implicit. | Plan lines 333-340, 391, and 396-472. | Mark provider protection as a manual release gate with an owner and recorded result. From a signed-out browser, prove denial for `/`, `/api/v1/config`, and one sensitive POST path before any real resume is used. Keep localhost as the fail-closed release outcome when this evidence is absent. |
| Question | Endpoint removal | Repository search proves that the SPA does not use the JSON ingestion route, but the route is documented in `README.md`. The plan treats absence of an in-repository caller as proof that no compatibility consumer exists. | Plan lines 85, 96, 199-201, and 487; `README.md` lines 87-96. | Record an explicit compatibility disposition before removal: either the documented API has no supported external consumer and this is an accepted breaking change, or keep a thin deprecated alias for one release. If removal proceeds, include API types, tests, README text, and release notes in the same change and rollback unit. |
| Minor | Rollback | Several rollback items restore only part of a coupled change. Restoring the JSON route without its client, OpenAPI types, tests, and documentation leaves contracts inconsistent. Removing the integration app without its CI command and package script leaves CI broken. Removing GSAP calls without removing the package leaves an unused production dependency. | Plan lines 489-496 and impacted-file lists in Phases 1, 2, and 4. | Define rollback units by coherent commit or file set. Each rollback must restore code, tests, generated contracts, documentation, package files, and CI together. |

## Missing Questions

- Is the documented `POST /api/v1/resumes/ingest` route guaranteed to have no supported consumer outside this repository?
- Which hosting-provider access method will protect the release, and who will record the signed-out denial check?

## Plan Feedback For Revision

- Add a Phase 0 and run Ponytail Full before all edits.
- Add request cancellation or generation guards to the target-change design, with a delayed-response regression test.
- Resolve the ignored synthetic PDF before CI design is accepted.
- Require one applied rewrite in the real-stack DOCX scenario and state what the repeated-byte check proves.
- Make the integration server working directory and served build artifact deterministic.
- Update `FUNCTIONAL_OVERVIEW.md` for editable target behavior and stale-state rules.
- Add exact GSAP cleanup and reduced-motion assertions.
- Make private-provider verification a manual release gate with recorded evidence.
- Define endpoint-removal compatibility and coherent rollback units.

## Confidence

High - The findings map to direct contradictions between the plan and the current reducer, request lifecycle, ignore rules, documentation, static-file selection, tests, and required workflow order.
